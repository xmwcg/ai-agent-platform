# 全功能可用性审计 · 达标清单 + 修复方案

> 审计日期：2026-07-22 ｜ 范围：前端 75 页面 + 后端 33 路由 + 支付变现闭环
> 目标：各功能各司其职、可上线、可变现、可商用、可运营（零虚拟数据、零占位死功能）

## 一、总览达标率

| 维度 | 达标率 | 死功能/断裂点 |
|---|---|---|
| 前端核心功能页（17） | 12✅ / 5🟡 | 5 处死交互（课时播放、订阅、AI提示、写死日历、工具开关） |
| 前端平台/变现页（21） | 19✅ / 1🟡 / 1🔴 | Profile 分销空表/复制无反应、Sandbox 默认 mock、JinWangTongDemo 纯演示 |
| 后端路由（33） | ~85% 真实 | 4 处内存态兜底（marketing/auth码/asset/relay密码） |
| 支付变现闭环 | ~82% | 4 处断裂（DEFAULT_PAY_PROVIDER耦合、任务积分假象、BYOK未落地、license dev缺口） |

**结论**：核心付费闭环与主流功能真实可用；缺陷集中在「死交互」+「生产内存兜底」+「支付耦合」，修复后达标率可升至 ≥95%。

---

## 二、达标清单（按模块，✅各司其职 / 🟡部分占位 / 🔴虚拟或异常）

| 模块 | 功能 | 状态 | 证据 |
|---|---|---|---|
| 知识中枢 | 知识库列表/详情/RAG/AI解读/代码解释 | ✅ | knowledgeAPI 全真实，含 loading/空态 |
| AI 对话 | AiChat / AibakChat | ✅ | aiAPI+gateway，失败回退 FALLBACK_MODELS |
| 学习中心 | 课程列表 | ✅ | /courses 真实 |
| 学习中心 | 课时播放 | 🔴 | CourseDetail:239 点击仅 toast，无播放器 |
| 学习中心 | 随堂测验 | 🟡 | QuizPage:344「需要提示」静态无 AI |
| 对比分析 | 模型对比 | ✅ | /compare/items + /generate 真实 |
| 模型日历 | 发布日历 | 🟡 | ModelCalendar:31 写死6条兜底；:133 订阅伪造 toast |
| 工具中心 | 翻译/方案/转换/媒体 | ✅ | toolsAPI 全真实 |
| 客服 | 在线客服 | ✅ | customerServiceAPI + chatPublic 真实 |
| 创作工坊 | 创意工坊 | 🟡 | CreativeWorkshop:69 工具开关本地态未持久化 |
| 定价 | 套餐/支付 | ✅ | PricingPage:181 真实建单→微信二维码→轮询 |
| 个人中心 | 订单/积分/BYOK | ✅ | billingAPI/pointsAPI/byokAPI 真实 |
| 个人中心 | 分销记录 | 🔴 | ProfilePage:354 dataSource={[]} 写死空 |
| 个人中心 | 复制邀请/取消订阅 | 🔴 | :340 无 onClick；:248 仅 message.info |
| 市场 | 技能/插件/MCP | ✅ | skillsAPI/mcpAPI/workflowAPI 真实导入 |
| 市场 | API Key 变现 | ✅ | marketplaceAPI 真实 |
| 团队 | RBAC | ✅ | teamAPI 真实 |
| 沙盒 | 代码执行 | 🟡 | SandboxPage:127 默认 mock，需 SANDBOX_REMOTE |
| 工作流 | 编排/执行 | ✅ | /api/workflows 增删改执行发布真实 |
| 积分 | 签到/流水/佣金 | ✅ | pointsAPI/referralAPI 真实（任务积分除外） |
| 分销 | 佣金/提现 | ✅ | DistributionPage 真实对接 |
| 管理后台 | 毛利/快照 | ✅ | /api/ops/snapshot 非写死 |
| 企业版 | 金王桶授权 | ✅ | JinWangTongPage:115 真实建单→二维码 |
| 企业版 | 演示页 | 🔴(预期) | JinWangTongDemo:186 纯演示，需标"演示"角标 |
| 法律页 | 条款/隐私等 | ✅ | 静态文案（预期内） |
| 后端 | auth/billing/chat/knowledge/course/compare/marketplace/team/workflow… | ✅ | MongoDB/Redis/模型/外部服务真实连接 |
| 后端 | marketing 意向 | 🔴 | marketing.ts:31 存内存 new Map()，多实例全丢 |
| 后端 | 验证码/重置码 | 🟡 | auth-verify:31 / auth-password:33 Redis 缺失静默内存 |
| 后端 | aibak 参考图 | 🟡 | asset-store:66 内存兜底，生产应强制 COS |
| 后端 | 中转站管理 | 🔴 | relay.ts:71 首次任意密码可抢注 |
| 支付 | 微信 v3 | ✅ | RSA2048 签名+证书验签+查单+幂等+重放防护 |
| 支付 | DEFAULT_PAY_PROVIDER | 🔴 | payment.service:467 忽略该变量，硬编码 NODE_ENV→mock |
| 积分 | 任务积分发放 | 🔴 | points.service:112 awardVerifiedTaskPoints 从未被调用 |
| BYOK | api_chat 优先自带Key | 🔴 | billing:215 声明但未在 ai.ts 注入，平台垫付 chat |

---

## 三、死功能 / 断裂点清单（精确位置 + 修复做法）

### P0 — 死功能清零（让网站"看起来死"的都修掉）
1. **CourseDetail.tsx:239** 课时点击仅 `message.info`，无播放器
   → 接入 CoursePlayer 路由（视频/文章详情组件）；无媒体则跳转 KnowledgeDetail 式内容页。
2. **ModelCalendar.tsx:31-38** `FALLBACK` 写死 6 条事件当真实数据
   → 删除写死兜底，空态显示"暂无公开事件"；厂商筛选空时 disabled。
3. **ModelCalendar.tsx:133** 「订阅推送」伪造示例 toast，无接口
   → 接 `/model-calendar/subscribe` 真实接口，或移除按钮。
4. **QuizPage.tsx:344** 「需要提示？」静态提示无真实 AI
   → 调 `aiAPI.chat` 传入题目上下文返回提示（复用现有网关）。
5. **ProfilePage.tsx:340** 复制邀请链接按钮无 `onClick`
   → 接 `navigator.clipboard.writeText(referralLink)` + message 成功提示。
6. **ProfilePage.tsx:354** 分销记录 `dataSource={[]}` 写死空
   → 接 `referralAPI.list()` 渲染真实佣金流水。
7. **ProfilePage.tsx:248** 取消订阅仅 `message.info('请联系客服')`
   → 调 `cancelSubscription` API（后端需补该端点或复用 `/billing/subscription`）。
8. **CreativeWorkshop.tsx:69-71** 工具启用开关本地态未持久化
   → 接后端配置接口保存；或明确标注为"导航页"移除开关。
9. **Home.tsx:370** 活跃用户/API 调用恒为 0 占位数字
   → 接 `/ops/public` 真实指标，或隐藏该两项统计。
10. **JinWangTongDemo.tsx:186** 演示扫描回显 mock
   → 页面右上角加"演示"角标（预期内，不修逻辑）。

### P1 — 支付硬化 + 生产合规（阻断真实收款/数据丢失）
11. **payment.service.ts:467** `getPaymentGateway()` 忽略 `DEFAULT_PAY_PROVIDER`，硬编码 `NODE_ENV!=='production'→'mock'`
   → 改为以 `DEFAULT_PAY_PROVIDER` 为准（缺省 production→wechat），移除 mock 误走；staging/preview 必须显式设 wechat 才放行。
12. **marketing.ts:31** 购买意向存内存 `new Map()`
   → 迁 MongoDB（新建 `marketingIntents` collection），多实例共享。
13. **auth-verify.ts:31 / auth-password.ts:33** 验证码/重置码 Redis 缺失时静默内存
   → Redis 不可用时硬失败 `throw`（生产绝不落内存 Map）。
14. **asset-store.ts:66-69** 参考图/转换产物内存兜底
   → env-check 强制 `COS_*` 或挂载共享卷，移除内存兜底分支。
15. **relay.ts:71** 中转站管理员密码可抢注
   → 生产预置强密码（env `RELAY_ADMIN_PASSWORD`），禁用"首次任意密码即初始化"。
16. **sandbox.service.ts:170** 非生产默认 mock
   → 生产强制 `SANDBOX_REMOTE_URL/TOKEN` 且启动时探测可达，失败则启动失败（不静默 mock）。

### P2 — 体验闭环补全（避免"假象"）
17. **points.service.ts:112** `awardVerifiedTaskPoints` 从未调用，任务积分展示为假象
   → 在 AI 对话完成/知识上传/课程完成/工具调用 等业务事件接入该发放；或下架 PointsCenter 任务积分展示避免误导。
18. **ai.ts / billing.ts:215** `api_chat` 声明 BYOK 优先但未注入用户 Key
   → 在 `ai.ts` 补 `byokBypass`+用户 key 注入（同 text2img 做法）；或从 `billing.ts:215` 移除 `api_chat` 声明。
19. **SandboxPage.tsx:127-133** 默认 mock 演示
   → 部署配真实 `SANDBOX_REMOTE`；无沙盒时明确标注"演示模式"不伪装真实。
20. **billing.ts:296,338** 私有化 license 在 mock/轮询路径仅标记已支付不签发
   → 补签发逻辑（复用 webhook 的 `issueLicense`），或文档标注"仅生产履约"。

---

## 四、修复后预期达标率

| 阶段 | 修复项 | 达标率 |
|---|---|---|
| 当前 | — | 前端~90% / 后端~85% / 支付~82% |
| +P0 | 10 项死功能清零 | 前端~96% / 整体~88% |
| +P1 | 6 项支付硬化+合规 | 支付~92% / 整体~93% |
| +P2 | 4 项体验补全 | 整体 **≥95%**（各功能各司其职，可运营） |

---

## 五、下一步 2 选项（问你同意哪个）

**选项1（建议）**：P0+P1+P2 一次性全量修复，连同服务器 502 恢复 + 本地 7 改动 commit→push→部署，一次到位达标 ≥95%。
- 结果：aibak.site 恢复 200、死功能清零、支付链路硬化、内存态迁 DB、积分/BYOK 闭环真实，各功能各司其职可运营。

**选项2**：先 P0+P1 快速上线（死功能清零 + 支付硬化 + 合规），P2（积分假象/BYOK/沙盒/license）排下迭代。
- 结果：先恢复上线、核心可用、无明显死功能，残留少量体验待办，达标率 ~93%。

**同意哪个？（只需回"1"或"2"）**
