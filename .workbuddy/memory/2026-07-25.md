# 工作日志 2026-07-25

## 修复本地微信支付故障 + 购买入口直接支付

### 根因
本地 `server/.env` 的微信支付 v3 全部字段被清空，且 `DEFAULT_PAY_PROVIDER=mock`。
→ 后端下单返回 mock 的 `payUrl`（非 `codeUrl`），前端拿不到二维码，
JinWangTongPage/JinWangTongDemo 落入"已创建订单，请在订单管理中完成支付 → 跳 /profile?tab=orders"分支。
这同时解释了两个现象：微信支付"故障" + "生成订单再让用户去找订单支付"。

### 配置来源（四端排查结论）
- 微信支付密钥**不在任何 git 仓库**（GitHub/CNB/本地历史均无，设计上要求存密钥库）。
- 真实值在**生产服务器** `/opt/ai-agent-platform/server/.env`（159.75.124.59，用 `~/.ssh/id_ed25519` 连接；`.ssh_id_ed25519`/`id_ed25519_aibak` 均非服务器key，后者是 cnb.cool 用）。
- 服务器当前健康（mongodb/redis connected，非 502）。

### 已恢复到本地 server/.env（该文件已 gitignore，勿提交）
WECHAT_MCH_ID=1616694790 / WECHAT_APP_ID=wx42150c26bd05fd2a / WECHAT_API_V3_KEY(32位) /
WECHAT_CERT_SERIAL=734C6584C1CA85C3859931CE484EE0CD21983E6F / WECHAT_PRIVATE_KEY / WECHAT_PLATFORM_CERT(有效期到2026-11-18) /
WECHAT_NOTIFY_URL=https://aibak.site/api/billing/webhook/wechat。
额外置：`DEFAULT_PAY_PROVIDER=wechat`、`PUBLIC_BASE_URL=https://aibak.site`
（微信 Native 下单要求 notify_url 为 https；本地到账靠 `/orders/:orderNo/status` 主动查单 `queryOrder` 兜底，无需公网回调）。
原 .env 已备份为 .env.bak-<时间戳>。

### 验证
- 本地脚本：私钥 RSA-SHA256 签名 OK、平台证书解析 OK。
- **真实调用微信 v3 Native 下单 HTTP 200，返回 code_url**（`weixin://wxpay/bizpayurl?pr=...`），凭据被微信接受 → 通道恢复。测试单1分、不扫码5分钟自动过期，无扣款。

### 代码改动（直接支付强化）
- `client/src/pages/JinWangTongPage.tsx`、`JinWangTongDemo.tsx`：把「无 codeUrl 时跳订单页」的兜底改为 `message.error('未获取到微信支付二维码…')`，
  确保购买入口只走"点击→弹微信二维码→扫码付→轮询自动到账"，绝不再把用户丢到订单页自己找单。
- PricingPage.tsx 本就是直接扫码流程，无需改。

### 关键机制备忘
- 后端 `payment.service.ts` WeChatPayGateway 用 env 读凭据；`createOrder` 的 notify_url 只取 `PUBLIC_BASE_URL`（**不读 WECHAT_NOTIFY_URL**）。
- dotenv 对双引号包裹的 `WECHAT_PRIVATE_KEY="...\n..."` 会展开 \n 为真实换行，Node crypto 可直接用。

### 后续（未做，待龙哥定）
- 未 commit/push/部署（本次仅修本地）。若要让服务器也保持一致——服务器本就是正确值，无需动。
- 全功能可用性审计里其它 P0/P1（见 2026-07-22 记录）仍待处理。
