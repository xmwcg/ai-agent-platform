# 降低并发与重试，缓解 GitHub Actions runner 上对庞大依赖树
# 偶发的 npm 内部崩溃（"Exit handler never called"，多为内存/资源争用）。
maxsockets=1
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=600000
fund=false
audit=false
