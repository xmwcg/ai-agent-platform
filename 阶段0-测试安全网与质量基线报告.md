# 阶段0 + 阶段1（P0 安全止血）· 测试安全网与质量基线报告

> 所属方案：ai-agent-platform 优化修复与开发方案（P0→P2 完整路线图，质量与测试并行起步）
> 日期：2026-07-15
> 状态：**已完成并通过验证门禁**

## 一、阶段0 目标

在动刀改造三个 P0 高危模块（`workflow-engine` / `mcp` / `sandbox`）之前，先建立两道"并行"防线：
1. **测试安全网**：为三个高危模块补 characterization test，锁定改造前行为，作为后续回归护栏。
2. **质量基线**：跑通类型检查 / 测试环境冒烟 / 依赖体检，记录现状，作为改造不退化的最低标准。

## 二、质量基线（改造前快照）

| 检查项 | 结果 | 说明 |
|---|---|---|
| `tsc --noEmit`（server） | **0 错误** | EXIT=0。修正了旧记忆"workflow-engine 有 TS1128 会致 tsc 整体失败"的判断——当前工作树类型干净 |
| jest 环境（ts-jest） | 正常 | `setup.ts` 仅 mock `ioredis` 与 `mongoose.connect`，不 mock model 操作 |
| 现有高危模块测试 | 缺口已定位 | `sandbox.service.ts` 原有 20 用例；`mcp.service.ts`、`workflow-engine.service.ts` 此前**无测试** |
| `strict` 编译选项 | false | 印证评估报告"any 滥用"判断，属阶段2 治理范围 |
| 根目录临时文件 | 存在（tsc_*.txt / jest_*.txt） | 按部署纪律**不提交**，仅作历史留痕 |

## 三、新增测试安全网（3 文件 / 38 用例）

### 1. `server/src/services/workflow-engine.service.test.ts`（9 用例）
锁定工作流引擎"拓扑编排 + 节点执行"现状：
- 拓扑串联 `input → code → output` 得到正确结果
- **code 节点（P0 高危）**：`new Function` 执行任意 JS、`typeof process` 可访问全局对象、语法错误降级为 error
- **condition 节点（P0 高危）**：`new Function` 表达式求值（真/假/{{input}} 替换/语法错误回退）

> 说明：code / condition 节点的"任意代码执行"被刻意以现状断言，作为基线快照，并在注释中明确标记为**阶段1a 待消除**。

### 2. `server/src/services/mcp.service.test.ts`（6 用例）
锁定 `connectStdio` 现状：
- 把 `config.command/args` **原样**透传给 `StdioClientTransport`（无白名单）
- **任意命令（含 `bash -c 'rm -rf /'`）均可连接**（P0 高危，标记为阶段1b 待加白名单）
- 非 stdio 传输 / 找不到服务器 → 抛错
- 连接失败 → 状态置 `error` 并向上抛
- `registerServer` 持久化（验证 `updateOne({id},{$set:{...}},{upsert:true})`）

### 3. `server/src/services/sandbox.service.test.ts`（原 20 + 新增 3 = 23 用例）
- 新增 local 模式三类行为：危险代码仍被 **deny-list 静态拦截**返回 `denied`、真实执行简单 JS 回显 stdout、代码抛错返回 `error`

> 关键发现：`sandbox.service.ts` 的 `LocalProvider`（line 225-282）**已有** `detectDangerousPatterns` 静态兜底——评估报告所说"无容器隔离"属实，但静态拦截存在。阶段1c 强化方向是**默认关闭 local 模式** + 危险模式硬拦截，而非从零补防护。

## 四、验证门禁结果（阶段0-6）

```
JEST_EXIT=0   （workflow 9 + mcp 6 + sandbox 23 = 38 用例全过）
TSC_EXIT=0    TSC_ERRORS=0（主体类型未退化）
```

✅ 阶段0 通过，已安全进入阶段1（P0 安全止血）。

## 五、阶段1 · P0 安全止血（已完成并通过验证）

### 1a · workflow-engine.service.ts（RCE 级）
- **改造前**：`condition` 节点（line 272）与 `code` 节点（line 286）直接用 `new Function('input', ...)` 执行任意 JS，等于工作流作者可在服务器执行任意代码（含 `process.exit`、读写文件）。
- **改造后**：
  - 引入 `vm` 受限沙盒（仅暴露 `input` 变量，不暴露 `process`/`require` 等宿主全局）+ 危险标识符黑名单（拦截 `constructor`/`process`/`require`/`Function`/`eval`/`new` 等）。
  - `condition` 节点改为受限表达式求值，**移除 `{{input}}` 字符串插值**（消除代码注入点），表达式通过 `input` 变量访问输入。
  - `code` 节点**默认禁用**（`WORKFLOW_CODE_NODE_ENABLED` 默认 `false`），需管理员显式开启；开启后仍受黑名单约束。
- 验证：characterization 用例同步改写为安全语义（默认禁用 / 启用后受限执行 / 危险标识符拦截均覆盖）。

### 1b · mcp.service.ts（服务端命令执行）
- **改造前**：`connectStdio` 把来自 DB 的 `config.command/args` 原样传给 `StdioClientTransport`，可运行 `bash -c 'rm -rf /'` 等任意命令。
- **改造后**：新增 `isAllowedStdioCommand` 命令白名单（`MCP_ALLOWED_STDIO_COMMANDS`，默认 `node,npx`），按 basename 校验以**防绝对路径绕过**；非白名单命令直接拒绝、状态置 `error`，绝不 spawn。
- 验证：白名单内 `node` 可连接；`bash`/`/usr/bin/curl` 等被拒；白名单命令的 MCP 握手失败仍正确置 `error`。

### 1c · sandbox.service.ts（本机无隔离执行）
- **改造前**：配置 `SANDBOX_MODE=local` 即在宿主机无容器执行用户代码（仅静态 deny-list 兜底）。
- **改造后**：新增 `SANDBOX_LOCAL_ENABLED` 开关（默认 `false`）；`selectSandboxMode` 对 `local` 默认降级为 `mock`，需管理员显式双重确认（`SANDBOX_MODE=local` 且 `SANDBOX_LOCAL_ENABLED=true`）才真正本机执行；`detectDangerousPatterns` 静态拦截在所有模式持续生效。
- 验证：默认 `mode:local` 请求降级为 mock；启用后真实执行且危险代码仍 `denied`。

## 六、P0 验证汇总

```
JEST_EXIT=0   （workflow 12 + mcp 7 + sandbox 24 = 43 用例全过）
TSC_EXIT=0    TSC_ERRORS=0（主线类型检查未退化）
```

✅ P0（三个高危点）已全部止血并通过验证门禁。

## 七、下一步

- **阶段2（质量治理）**：巨型文件拆分、any 收敛、清理未用依赖、补关键模块测试。
- **阶段3（增强）**：可观测性（结构化日志/指标/告警）、安全文档、前端巨型组件拆分、开放市场/技能生态。
- **部署提示**：本次仅改 `server/src/services/` 三个文件 + 新增/改写测试，不涉及启动门禁（`env-check.ts` 的 `ENCRYPTION_KEY` 不受影响）。生产部署按纪律**外科手术式精准提交**，绝不整推；新增环境变量 `WORKFLOW_CODE_NODE_ENABLED` / `MCP_ALLOWED_STDIO_COMMANDS` / `SANDBOX_LOCAL_ENABLED` 均为可选（默认即安全态）。
